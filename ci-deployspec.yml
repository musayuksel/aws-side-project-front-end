version: 0.2

phases:
  build:
    commands:
      - echo "Debugging directory structure"
      - pwd
      - ls -la
      - echo "Listing the content of the BuildOutput artifact"
      - ls -la $CODEBUILD_SRC_DIR/BuildOutput
      - echo "Deploy phase, attempting to find and deploy artifacts"
      - aws s3 sync $CODEBUILD_SRC_DIR/BuildOutput/ s3://musa-website-test-bucket-123123/ --delete
      - echo "Deployment to S3 complete"